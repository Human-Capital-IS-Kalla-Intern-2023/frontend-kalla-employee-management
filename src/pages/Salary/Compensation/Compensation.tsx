import CompensationCard from '../../../components/cards/Compensation/CompensationCard';
const Compensation = () => {
  return (
    <>
      <CompensationCard />
    </>
  );
};

export default Compensation;
